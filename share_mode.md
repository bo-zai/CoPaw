### 一、整体交互模式

用户点击聊天页面中的“分享”入口后，页面进入一个独立的 **Share Selection Mode / 分享选择模式**。

进入该模式后：

* 聊天内容本身不重新排版，仍保持原来的消息结构和顺序。
* 每一条“可分享消息”左侧出现一个方形复选框。
* 页面底部出现固定的分享操作栏。
* 用户可以上下滚动聊天记录，并持续选择需要分享的消息。
* 选择完成后，通过底部操作栏决定具体分享方式。
* 点击右下角的“×”退出分享模式，并恢复普通聊天界面。

整体逻辑可以概括为：

**进入分享模式 → 多选消息 → 选择分享渠道 → 执行分享 / 生成分享内容 → 退出分享模式**

---

## 二、消息区域 UI

截图里最大的特点是：**没有为了分享模式重做聊天 UI，只是在现有消息左侧增加选择能力。**

### 1. 复选框位置

每个可选消息块左侧增加一个黑色方形 Checkbox。

选中状态：

* 黑色实心背景；
* 内部白色对勾；
* 尺寸较小，大约 20～24 px；
* 圆角约 4～6 px。

位置不是完全贴着消息框，而是位于：

> 页面左侧固定选择列 + 原消息内容区域

可以理解为聊天区域临时变成：

```text
┌────────┬────────────────────────────────────────────┐
│ Checkbox │             原聊天内容                    │
└────────┴────────────────────────────────────────────┘
```

这样做有一个重要好处：

**进入分享模式不会引起聊天气泡本身大幅位移或重新布局。**

---

### 2. 用户消息

用户消息仍然维持原来的右对齐形式。

截图中的用户消息是：

* 浅灰色长圆角矩形；
* 靠右排列；
* 左侧留出头像/选择列空间；
* Checkbox 位于整条用户消息的左侧，而不是紧挨气泡。

视觉结构大致是：

```text
☑        ┌────────────────────────────────────────────┐
         │                    用户消息                │
         └────────────────────────────────────────────┘
```

也就是说：

**Checkbox 对应的是“整条消息”，而不是消息中的某段文字。**

---

## 三、助手消息选择方式

助手回复采用同样的选择机制。

截图中：

```text
      WorkBuddy
☑     ┌──────────────────────────────────────┐
      │ 助手回复内容                         │
      │                                      │
      │ 文件、文本、过程消息等               │
      └──────────────────────────────────────┘
```

这里有一个值得复刻的细节：

### Checkbox 对应的是整个 Assistant Message Block

也就是说，一次助手回复中即使包含：

* 正文；
* Markdown；
* 文件卡片；
* 工具执行结果；
* “过程消息”；
* 附件；

从分享选择角度看，依然作为一个整体 Message Unit。

这样比让用户选择回复内部的每个区块简单很多。

因此建议你的数据模型中，把选择粒度控制在：

> **Shareable Answer Turn**

而不是：

> Content Block。

否则分享操作会变得非常碎片化。

---

# 四、选中状态

从截图看，进入分享模式后，所有截图里的消息均处于选中状态，因此 Checkbox 都是：

```text
☑
```

本项目采用会话级分享入口，进入分享模式时默认选中所有可分享的完整 Answer Turn。

### 已确认策略：默认全部选中

用户点击“分享会话”时：

```text
进入分享模式
↓
当前范围消息全部选中
↓
用户取消不需要的消息
```

用户可以取消任意轮次，最终分享内容允许是不连续的 Answer Turn 集合。

适合：

* 分享整段聊天；
* 分享报告；
* 分享完整上下文。

截图比较像这种行为。

用户输入和模型输出必须成对选择。两条消息各显示一个 Checkbox，但它们属于同一个 Answer Turn：点击任一 Checkbox 都会原子切换整轮，不允许出现只选输入或只选输出的状态。

运行中、停止中、失败或缺少模型输出的轮次不可选；没有可分享轮次时仍进入模式并展示空状态。

---

# 五、底部分享操作栏

这是整个分享模式中最明显的新 UI。

底部出现一个固定白色操作区域，始终覆盖在聊天内容之上。

可以理解成：

> Fixed Bottom Share Toolbar

而不是普通弹窗。

结构大约为：

```text
┌──────────────────────────────────────────────────────┐
│                                                      │
│                 分享渠道                              │
│                                                      │
│                 复制链接    浏览器打开               │
│                                                      │
│ ☑ 全选                                           ×   │
└──────────────────────────────────────────────────────┘
```

但截图中的实际布局更接近：

```text
                         ○      ○
                      复制链接   浏览器打开

☑ 全选                                                ×
```

---

# 六、分享渠道设计

本项目只保留两个操作，均复用同一个 Shared Conversation URL。

### 复制链接

链条图标。

文案：

> 复制链接

点击后：

```text
生成 Share Token
↓
生成分享链接
↓
写入 Clipboard
↓
Toast：链接已复制
```

这是最值得优先实现的一种分享方式。

---

### 浏览器打开

地球图标。

文案：

> 浏览器打开

作用应该是：

> 在浏览器中预览最终分享页面。

分享模式本身使用固定底部操作栏。链接按需生成：进入模式和调整选择不会创建快照；首次点击任一操作时按当前选择创建链接，两个操作复用该 URL。选择发生变化后缓存失效，下次操作生成新的永久链接。成功后退出分享模式；点击“×”直接退出且不创建链接。

对于复刻系统而言，这个功能非常有价值，因为用户可以先看到：

**别人最终看到的分享效果。**

---

# 七、“全选”交互

底部左侧有：

```text
☑ 全选
```

这实际上承担了两个功能：

### 当前并非全部选中

显示：

```text
□ 全选
```

点击：

```text
Select All
```

### 当前全部选中

显示：

```text
☑ 全选
```

再次点击：

```text
Deselect All
```

如果消息比较多，还可以进一步设计成三态 Checkbox：

```text
□   未选择

▣   部分选择

☑   全部选择
```

但截图中没有明显表现出三态。

如果目标是保持简单，可以只做：

**全选 / 全不选。**

---

# 八、关闭分享模式

右下角：

```text
×
```

作用不是关闭底部面板，而是：

> **退出整个 Share Mode。**

点击后：

* 隐藏所有 Checkbox；
* 隐藏底部 Toolbar；
* 恢复普通聊天状态；
* 原聊天滚动位置保持不变。

这一点很重要。

不要退出分享模式后把页面重新滚动到顶部或者重新加载聊天记录。

---

# 九、滚动行为

从截图判断，分享操作栏固定在窗口底部，而聊天内容仍然可以滚动。

因此整体结构应该类似：

```text
Conversation Container
│
├─ Scrollable Message List
│
│   ☑ Message
│   ☑ Message
│   ☑ Message
│   ☑ Message
│
└─ Fixed Share Toolbar
```

底部 Toolbar 使用类似：

```css
position: fixed;
bottom: 0;
```

同时 Chat List 需要增加：

```text
padding-bottom
```

避免最后一条消息完全被分享栏遮挡。

截图中底部聊天内容实际上被 Share Toolbar 部分覆盖，这可能是原产品有意做出的“悬浮层”效果，但你在新系统里最好预留一定 Safe Area。

---

# 十、视觉风格

这套 UI 的特点不是“强分享感”，而是非常克制。

### 页面背景

纯白或者接近：

```text
#FFFFFF
```

### 消息气泡

浅灰：

```text
#F3F3F3
~
#F5F5F5
```

### 分享 Toolbar

白色背景，并带有轻微：

* 顶部阴影；
* 模糊；
* 渐变遮罩。

视觉上不是一条明显边框，而是：

```text
聊天内容
   ↓
逐渐变白
   ↓
Share Toolbar
```

因此可以使用：

```text
box-shadow:
0 -12px 30px rgba(...)

backdrop-filter:
blur(...)
```

但不要让阴影太明显。

---

# 十一、分享模式最好保持的状态

如果你要完整复刻，我建议定义一个单独的：

```text
Normal Mode
        ↓
Share Selection Mode
```

Share Mode 内至少维护：

```text
selectedMessages
shareScope
scrollPosition
```

而不是修改原始聊天数据。

即：

```text
Conversation
    │
    ├── Message A
    ├── Message B
    ├── Message C
    └── Message D

Share State
    │
    └── selected = [A, C, D]
```

分享模式只是 UI 状态。

这样退出分享以后聊天状态完全不受影响。

---

# 十二、建议你复刻成这样的完整交互

如果换成产品语言描述，可以定义为：

> 用户点击“分享”后进入会话多选模式。聊天页面保持原有布局，每条可分享消息左侧展示复选框；用户输入与模型输出属于同一个 Answer Turn，两个复选框状态始终同步，只能整轮勾选或取消。进入模式时默认选中全部已完成轮次，用户可以取消部分轮次。页面底部固定展示分享操作栏，左侧提供“全选”，中部仅提供“复制链接”和“浏览器打开”，右侧提供退出按钮。用户滚动会话时分享操作栏保持固定，选择状态持续保留。链接按需生成，选择变更后失效；执行成功后退出分享模式。

从交互架构上看，可以抽象成：

```text
普通会话模式
      │
      │ 点击分享
      ▼
消息多选模式
      │
      ├── 单条选择
      ├── 多条选择
      ├── 全选 / 全不选
      │
      ▼
生成 Share Snapshot
      │
      ├── Copy URL
      └── Browser Preview
```

其中最值得复刻的核心不是底部按钮数量，而是 **“原聊天界面直接进入多选状态”** 这一设计。它避免了“先进入分享页 → 再编辑分享内容”的额外步骤，非常适合 Agent、AI Chat、工作助手这一类系统。

---

## 十三、已确认的 Console UI 修订

以下决定以截图的布局、间距和操作形态为视觉参考，但不扩展分享渠道：

* 分享功能仍只提供“复制链接”和“浏览器打开”两个渠道。
* 普通模式和分享模式均不显示“新建会话”图标；“分享会话”入口始终保留。
* 每个 Answer Turn 的用户消息和助手消息各显示一个 Checkbox，两个状态原子同步；只能点击 Checkbox 选择，消息气泡本身不高亮。
* Checkbox 使用蓝色选中、灰色边界、白色背景/符号的配色：选中和半选为品牌蓝色实心底配白色符号，未选为白底灰边，禁用状态降低对比度。
* 进入分享模式默认选中全部已完成轮次；“全选”支持未选、半选、全选三态，不显示已选数量。
* 底部操作栏固定覆盖整个视口：左侧为“全选”，中部为两个图标在上、文字在下的垂直操作项，右侧为关闭按钮。面板覆盖原对话输入框，但消息滚动区域预留底部安全间距。
* 分享模式隐藏/禁用输入框及发送、上传等状态变更操作；退出后恢复。
* 空会话仍进入分享模式，面板显示“暂无可分享内容”，选择和分享操作禁用。
* 链接仅在执行分享操作时按当前选择生成；选择变化立即清除缓存。生成或打开失败时保持分享模式，成功后退出；关闭按钮不创建链接。

如果你的另一个系统同样是 Agent 会话界面，我会建议进一步把**文件、工具调用、过程消息是否纳入分享**作为分享规则处理，而不要继续增加选择层级，否则 UI 很容易变复杂。
